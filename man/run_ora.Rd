% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ora.R
\name{run_ora}
\alias{run_ora}
\title{Run a GO or KEGG over-representation analysis (ORA)}
\usage{
run_ora(
  df = NULL,
  contrast = NULL,
  DE_direction = "either",
  focal_genes = NULL,
  term_map = NULL,
  OrgDb = NULL,
  ontology_type = NULL,
  organism = NULL,
  keyType = "ENTREZID",
  GO_ontology = NULL,
  p_enrich = 0.05,
  q_enrich = 0.2,
  min_DE_in_cat = 2,
  min_cat_size = 5,
  max_cat_size = 500,
  filter_no_descrip = TRUE,
  exclude_nontested = TRUE,
  universe = NULL,
  allow_dups = FALSE,
  simplify_terms = FALSE,
  simplify_cutoff = 0.7,
  return_df = FALSE,
  p_DE = 0.05,
  verbose = TRUE
)
}
\arguments{
\item{df}{DE results df with at least columns \code{gene}, \code{contrast},
\code{lfc}/\code{log2FoldChange}, \code{padj}. This df should contain \emph{all} genes, not
just significantly DE ones. Can also be a raw DESeq2 \code{results()} object
(class \code{DESeqResults}) -- it is converted automatically and tagged with
the \code{contrast} argument below.}

\item{contrast}{DE comparison as specified in the \code{contrast} column in DE
results.}

\item{DE_direction}{DE direction: \code{"either"} (all DE genes), \code{"up"} (lfc >
0), or \code{"down"} (lfc < 0).}

\item{focal_genes}{Alternative to \code{df}: a pre-built vector of focal gene
IDs.}

\item{term_map}{Manually provide a functional category/term to gene
mapping with columns: 1: term, 2: gene, and optionally 3: description,
4: ontology.}

\item{OrgDb}{Bioconductor \code{OrgDb} (alternative to \code{term_map}, for GO
enrichment in available organisms). Required when \code{ontology_type = "GO"}; not used for \code{ontology_type = "KEGG"} (see \code{organism}).}

\item{ontology_type}{\code{"GO"} (\code{enrichGO()}, requires \code{OrgDb}) or \code{"KEGG"}
(\code{enrichKEGG()}, requires \code{organism}). Only applies when using an
\code{OrgDb}/\code{organism} instead of a \code{term_map}.}

\item{organism}{KEGG species code (e.g. \code{"hsa"} for human, \code{"mmu"} for
mouse; see \url{https://rest.kegg.jp/list/organism} for the full list).
Alternative to \code{term_map}, used instead of \code{OrgDb} when \code{ontology_type = "KEGG"}.}

\item{keyType}{OrgDb gene ID type (\code{ontology_type = "GO"}) or
\code{enrichKEGG()} \code{keyType} (\code{ontology_type = "KEGG"}) -- only applies when
using an \code{OrgDb}/\code{organism} instead of a \code{term_map}. For KEGG, the
default \code{"ENTREZID"} is translated to \code{enrichKEGG()}'s own
\code{"ncbi-geneid"}; any other value is passed through as-is, but must be
one KEGG's own API supports: \code{"kegg"}, \code{"ncbi-geneid"},
\code{"ncbi-proteinid"}, or \code{"uniprot"} (notably \emph{not} \code{"ENSEMBL"} -- convert
Ensembl IDs to one of these first, e.g. via \code{AnnotationDbi::mapIds()}).}

\item{GO_ontology}{Only applies when \code{return_df == FALSE} (default \code{"BP"}
there, since clusterProfiler can't combine multiple ontologies into a
single \code{enrichResult}); ignored when \code{return_df == TRUE}, which always
runs GO analyses across all 3 ontologies (BP, CC, MF).}

\item{p_enrich}{Adjusted p-value threshold for enrichment.}

\item{q_enrich}{Q value threshold for enrichment.}

\item{min_DE_in_cat}{Number-of-DE-genes threshold for enrichment: at least
this number of genes in the ontology category should be DE.
(Occasionally, 'small' categories with 1 DE gene can have p-values below
0.05 -- this excludes those.)}

\item{min_cat_size}{Min. number of genes in a category (=
clusterProfiler's \code{minGSSize} argument; note that clusterProfiler's own
default is 10).}

\item{max_cat_size}{Max. number of genes in a category (=
clusterProfiler's \code{maxGSSize} argument).}

\item{filter_no_descrip}{Remove categories/terms with no description (at
least for GO terms, these tend to be old/deprecated ones).}

\item{exclude_nontested}{Exclude genes that weren't tested (i.e., have
\code{NA} in the \code{padj} column) for DE from the \code{universe} of genes.}

\item{universe}{Manually specify a background \code{universe} of genes (vector
with gene IDs).}

\item{allow_dups}{Allow a gene ID to be present multiple times in a
(single-contrast, single DE-direction) list of DEGs. This should
typically \emph{not} be the case, but could be so when working with gene IDs
(orthologs) from another species than the focal species to run the GO
analysis.}

\item{simplify_terms}{Whether to use clusterProfiler's \code{simplify()}
function to flag redundant/similar GO terms among the significant ones.
All tested terms are always returned (regardless of \code{simplify_terms})
in a single \code{redundant} column: \code{NA} for terms that weren't significant
(\code{padj < p_enrich}, \code{qvalue < q_enrich}, \code{Count >= min_DE_in_cat}),
\code{FALSE} for significant terms kept by \code{simplify()} (or when
\code{simplify_terms = FALSE}), and \code{TRUE} for significant terms removed by
\code{simplify()} as too similar to another, more significant term.}

\item{simplify_cutoff}{Simplify similarity cutoff.}

\item{return_df}{Convert the result object to a simple dataframe (tibble),
instead of keeping the clusterProfiler object. Should be \code{FALSE} if you
want to use the enrichPlot functions directly.}

\item{p_DE}{Adjusted p-value threshold used to derive \code{isDE} from \code{df}
when \code{df} has no \code{isDE} column.}

\item{verbose}{Print progress messages (Contrast/DE direction/gene
counts, \code{simplify()} summary, enriched-term count).}
}
\value{
An \code{enrichResult} object (\code{return_df = FALSE}), a tibble
(\code{return_df = TRUE}), or \code{NULL} when there were too few (or no mapped)
genes to run the enrichment analysis.
}
\description{
A wrapper around \code{clusterProfiler::enricher()}/\code{clusterProfiler::enrichGO()}
that derives a focal (DE) gene set and a background universe from a
differential expression results table (or a directly supplied gene
vector), runs the enrichment analysis, and optionally flags redundant
terms and/or converts the result to a tidy tibble.
}
