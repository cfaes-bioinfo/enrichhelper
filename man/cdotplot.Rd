% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdotplot.R
\name{cdotplot}
\alias{cdotplot}
\title{Cleveland dotplot of enrichment results}
\usage{
cdotplot(
  df,
  contrasts = NULL,
  DE_dirs = NULL,
  x_var = "padj_log",
  fill_var = "median_lfc",
  label_var = "n_focal_in_cat",
  facet_var1 = NULL,
  facet_var2 = NULL,
  facet_to_columns = TRUE,
  facet_scales = NULL,
  facet_label_fun = "label_value",
  x_title = NULL,
  ylab_size = 10,
  add_term_id = FALSE,
  point_size = 6,
  label_chars = 60,
  scico_palette = NULL
)
}
\arguments{
\item{df}{Dataframe with enrichment results from \code{\link[=run_ora]{run_ora()}}, already
filtered to the rows to plot (e.g. significant, non-redundant terms).}

\item{contrasts}{One or more contrasts (default: all).}

\item{DE_dirs}{One or more DE directions (default: all).}

\item{x_var}{Column in \code{df} to plot along the x axis (\code{"padj_log"} will
be computed from \code{padj}).}

\item{fill_var}{Column in \code{df} to vary fill color by (\code{"padj_log"} will
be computed from \code{padj}).}

\item{label_var}{Column in \code{df} with a number to add as a label in the
circles.}

\item{facet_var1}{Column in \code{df} to facet by.}

\item{facet_var2}{Second column in \code{df} to facet by (e.g. \code{"ontology"}
for GO).}

\item{facet_to_columns}{When only using one \code{facet_var1}, facets are
columns (or rows).}

\item{facet_scales}{Facet scales: \code{"fixed"}, \code{"free"}, \code{"free_x"}, or
\code{"free_y"}.}

\item{facet_label_fun}{Function to label facets with.}

\item{x_title}{X-axis title.}

\item{ylab_size}{Size of y-axis labels (= term labels).}

\item{add_term_id}{Add term ID (e.g., \code{"GO:0009539"}) to its description.}

\item{point_size}{Point size.}

\item{label_chars}{Truncate the term labels to this many characters (+10
when including the term ID).}

\item{scico_palette}{Name of a \code{scico::scale_color_scico()} palette
(e.g. \code{"batlow"}) to use for a numeric \code{fill_var} instead of the
default viridis scale (ignored for \code{fill_var \%in\% c("mean_lfc", "median_lfc")}, which always use the diverging scale).}
}
\value{
A \code{ggplot} object.
}
\description{
Plots a tidy enrichment-results tibble (e.g. from \code{\link[=run_ora]{run_ora()}} or
\code{\link[=cp_to_df]{cp_to_df()}} with \code{return_df = TRUE}) as a Cleveland dotplot, with one dot
per row of \code{df}. \code{cdotplot()} does not filter for significance or
redundancy itself -- filter \code{df} (e.g. on a \code{redundant} column: \code{NA} =
not significant, \code{FALSE} = significant & kept, \code{TRUE} = significant but
removed by \code{simplify()}) before calling this function.
}
