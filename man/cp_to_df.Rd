% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ora.R
\name{cp_to_df}
\alias{cp_to_df}
\title{Convert a clusterProfiler enrichment result to a tidy dataframe}
\usage{
cp_to_df(
  res,
  df = NULL,
  term_map = NULL,
  contrast = NA,
  DE_direction = NA,
  p_enrich = 0.05,
  q_enrich = 0.2,
  min_DE_in_cat = 2
)
}
\arguments{
\item{res}{A \code{clusterProfiler} \code{enrichResult} (S4 object), or a tibble
already built via \code{purrr::map_dfr()}.}

\item{df}{Original DE results (needs \code{gene} + \code{lfc} columns); when
supplied, adds \code{mean_lfc}/\code{median_lfc} per term.}

\item{term_map}{Optional term-to-gene map; when it has an \code{ontology}
column, adds an \code{ontology} column (term_map path).}

\item{contrast}{Value written into the \code{contrast} column.}

\item{DE_direction}{Value written into the \code{DE_direction} column.}

\item{p_enrich}{Adjusted p-value threshold for the \code{sig} flag.}

\item{q_enrich}{Q-value threshold for the \code{sig} flag.}

\item{min_DE_in_cat}{Min. number of DE genes in a category for the \code{sig}
flag.}
}
\value{
A tibble with one row per tested term.
}
\description{
Converts a \code{clusterProfiler} enrichment result to a tidy tibble. This is
the same conversion \code{\link[=run_ora]{run_ora()}} applies internally when \code{return_df = TRUE}, exposed separately so it can be used on a \code{clusterProfiler} result
produced outside \code{\link[=run_ora]{run_ora()}} (e.g. a direct \code{enricher()}/\code{enrichGO()}
call) and fed to \code{\link[=cdotplot]{cdotplot()}}.
}
